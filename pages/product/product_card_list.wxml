<view>
  <mp-cells title="卡类产品列表" >
    <mp-cell wx:for="{{productList}}"   >
      <view>{{item.name}}</view>
      <view>{{item.sub_title}}</view>
      <view>{{item.desc}}</view>
      <view><button id="{{item.id}}" bindtap="submit" >购买</button></view>
    </mp-cell>
  </mp-cells>
</view>